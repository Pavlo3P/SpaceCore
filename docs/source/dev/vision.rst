Vision
======

The canonical vision document lives in ``docs/dev/vision.md``. It is included
here so it is reachable from the built developer documentation without adding a
Markdown parser dependency to Sphinx.

.. literalinclude:: ../../dev/vision.md
   :language: markdown
