# SpaceCore 0.3.1 release-candidate report

## Environment

- Python: 3.12.9 (`.venv312`)
- OS: Darwin 25.5.0 arm64 (`Pavlos-MacBook-Pro.local`)
- Date: 2026-06-09 America/Sao_Paulo
- Core dependencies installed: NumPy, SciPy, array-api-compat
- Optional dependencies installed for checks: JAX, Torch, Sphinx, Ruff, Twine, nbconvert, ipykernel, matplotlib, Optax
- Optional dependencies not installed: CuPy
- Wheel-check environment: `.venv-wheel-check`, Python 3.12, installed from `dist/spacecore-0.3.1-py3-none-any.whl`

## Checks

- Package import: passed; `spacecore.__version__ == "0.3.1"`
- Tutorials: passed; all active notebooks in `tutorials/*.ipynb` executed with `.venv312/bin/jupyter-nbconvert --to notebook --execute`, outputs written to `/tmp/*-py312-check.ipynb`
- Worked example: passed; `examples/weighted_tikhonov.py` executes and validates against the independent dense NumPy reference
- Public API documentation audit: passed; `scripts/api_audit.py .` reported no removed-API findings outside historical migration notes
- Public docstring audit: passed; `scripts/docstring_audit.py --check` reported `numpydoc issues: 0`
- Lint: passed; `.venv312/bin/ruff check .`
- Formatting: passed; `.venv312/bin/ruff format --check .`
- Tests: passed; `.venv312/bin/python -m pytest` reported `660 passed, 47 skipped, 6 warnings`
- Doctests: passed as part of configured pytest doctest module run
- Strict docs build: passed; `.venv312/bin/sphinx-build -W --keep-going -b html docs/source docs/build/html`
- Build artifacts: passed; `.venv312/bin/python -m build` built sdist and wheel
- Twine check: passed; `.venv312/bin/python -m twine check dist/*`
- Artifact inspection: passed; sdist includes source, tests, docs, tutorials, examples, changelog, release notes, README, license, and metadata; wheel contains package files and metadata only
- Clean wheel installation: passed; installed `dist/spacecore-0.3.1-py3-none-any.whl` into `.venv-wheel-check`
- Smoke test: passed from `/tmp`; imported installed package, constructed dense and product spaces, applied a dense operator and its adjoint, and solved a small CG system

## Artifacts

- sdist: `dist/spacecore-0.3.1.tar.gz` (365138 bytes)
- wheel: `dist/spacecore-0.3.1-py3-none-any.whl` (132398 bytes)

## Known limitations

- Solver coverage remains intentionally narrow: CG, LSQR, power iteration, Lanczos smallest-eigenpair estimation, and matrix-exponential actions.
- Optional backend support depends on installed optional dependencies; CuPy was not installed for this release-candidate run.
- The regularized optimal transport tutorial is illustrative and depends on optional JAX/Optax tooling; it is not a production OT solver.
- SDPLab-specific downstream validation was intentionally not run for this SpaceCore release-candidate task.

## Follow-up issues

- Add tiny examples to public docstrings that are currently prose-covered but lack explicit `Examples` sections, as recorded in `docs/dev/0.3.1-docstring-audit.md`.
- Decide whether ignored local guide material under `guides/` should be revived, deleted, or moved into tracked documentation in a separate documentation cleanup.
